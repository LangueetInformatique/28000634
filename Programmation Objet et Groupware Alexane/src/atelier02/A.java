package atelier02;

public class A {
	// la classe A est utilisée dans le testAB
		// A contient la méthode treize retournant la valeur true si un entier n passé en paramètre est divisible par 13 (false dans le cas contraire)//
		
		public static boolean treize(int n) {
		
		if ((n % 13 ) == 0)
		return true;
		else return false;
		}
	}
